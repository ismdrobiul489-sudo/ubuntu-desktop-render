# render.yaml - Render Blueprint

services:
  - type: web
    name: ubuntu-desktop
    env: docker
    plan: starter  # or standard for more resources
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
